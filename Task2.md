## 动作执行引擎

### 目标

做一个“执行工人”，负责把任务一生成的动作真正执行掉。

任务二不需要理解复杂业务逻辑，只需要看 action_task 里写了什么，然后执行。

### 主要功能

| 功能 | 说明 |
|---|---|
| 读取待执行任务 | 从 action_task 或 Redis 队列读取任务 |
| 发送链接 | 给客户发送排队链接 |
| 发送固定话术 | 按动作类型发送对应话术 |
| 发送视频 | 到时间只发送视频 |
| 发送付款链接 | 视频后6分钟固定发送 |
| 发送付款二维码 | 客户付不了款时发送 |
| 登记客户 | 把客户资料写入登记系统 |
| 创建视频任务 | 调用视频生成软件/API/RPA |
| 定时任务 | 处理2分钟、20分钟、6分钟延迟动作 |
| 执行结果回传 | 成功、失败、客户删除、无法发送等结果回传任务一 |

### 需要支持的动作类型

| 动作类型 | 执行动作 |
|---|---|
| SEND_LINK | 发送排队链接 |
| ASK_FOR_IMAGE_INFO | 发送“请把上面图片的信息发我一下” |
| ASK_MISSING_BIRTHDAY | 发送“把出生年月日发我一下” |
| ASK_MISSING_NAME | 发送“把姓名发我一下” |
| ASK_CLICK_LINK | 发送“点链接参与排队” |
| SEND_WAIT_20_MIN_MESSAGE | 发送“好的，请稍等20分钟左右” |
| REGISTER_CUSTOMER | 登记客户资料 |
| CREATE_VIDEO | 创建视频生成任务 |
| SEND_VIDEO | 发送视频 |
| SEND_PAYMENT_LINK | 发送付款链接 |
| SEND_PAYMENT_QR | 发送付款二维码 |
| SEND_RED_PACKET_REPLY | 发送红包拒收话术 |
| ASK_DONATION_AMOUNT | 发送“随喜了多少呢？” |
| FINISH_TASK | 结束任务，不再执行后续动作 |

### 任务二可以先用的工具

| 场景 | 优先方案 | 兜底方案 |
|---|---|---|
| 发企业微信消息 | 企业微信API | RPA模拟发送 |
| 读取客户消息 | 会话存档/API | RPA识别窗口 |
| 链接点击 | 自建短链 | 落地页统计 |
| OCR截图 | PaddleOCR / 云OCR | 人工复核 |
| 登记资料 | 后台接口 | RPA填表 |
| 视频生成 | 视频软件API | RPA操作软件 |
| 定时任务 | Redis + BullMQ | 数据库轮询 |
| 付款链接 | 固定链接/支付API | 手动配置链接 |
| 付款二维码 | 素材库 | 人工上传 |

### 任务二完成标准

任务二完成后，需要能做到：

- 能读取 action_task。
- 能按 action_type 执行动作。
- 能处理延迟任务。
- 能回传执行结果。
- 发送失败时能标记失败原因。
- 客户删除/无法触达时能通知任务一结束任务。

## 4. 两个模块怎么拼起来

### 拼接方式

统一用 `action_task` 表或队列作为连接口。

```text
任务一：客户状态中台
  -> 写入 action_task

任务二：动作执行引擎
  -> 读取 action_task
  -> 执行动作
  -> 回写执行结果

任务一：根据结果更新 customer_task
```

### 示例

客户没点链接但发了资料：

```text
任务一判断：
  状态 = 未点链接 + 有用资料
  生成动作 = ASK_CLICK_LINK
  同时生成延迟动作 = 2分钟后 CHECK_LINK_CLICKED

任务二执行：
  发送“点链接参与排队”
  2分钟后触发检查

任务一再次判断：
  如果已点链接 -> 登记
  如果没点链接但资料有效 -> 也登记
```

视频发送后的付款链接：

```text
任务二发送视频成功
  -> 回传 VIDEO_SENT

任务一更新状态：
  current_status = VIDEO_SENT
  生成延迟动作 = 6分钟后 SEND_PAYMENT_LINK

任务二6分钟后执行：
  发送付款链接
```

客户删除：

```text
任务二发送失败，返回 CUSTOMER_DELETED

任务一更新状态：
  current_status = CUSTOMER_DELETED
  final_status = FINISHED
  取消所有后续 action_task
```

## 5. 两个团队或两个人怎么分工

### 人/团队 A：做客户状态中台

负责：

- 数据库设计。
- 客户状态机。
- 业务规则判断。
- 资料完整度判断。
- action_task 生成。
- 任务结束逻辑。
- 管理后台里的客户状态展示。

不负责：

- 真正发消息。
- 真正操作视频软件。
- 真正发送付款链接。

### 人/团队 B：做动作执行引擎

负责：

- 读取 action_task。
- 对接企业微信/RPA。
- 发送话术。
- 发送视频。
- 发送付款链接和二维码。
- 调用视频生成软件。
- 回传执行结果。

不负责：

- 判断客户业务状态。
- 决定下一步该做什么。
- 管理复杂业务规则。

## 6. 最小可拼接版本 MVP

第一版不要做太复杂，两个模块只要约定好接口就行。

### MVP 任务一

只做：

- 新客户创建。
- 链接状态。
- 资料完整度判断。
- 生成动作任务。
- 客户状态展示。

### MVP 任务二

只做：

- 发送链接。
- 发送固定话术。
- 发送视频。
- 6分钟后发付款链接。
- 回传发送结果。

### MVP 接口

```json
{
  "customer_task_id": "CT_001",
  "action_type": "SEND_WAIT_20_MIN_MESSAGE",
  "payload": {
    "text": "好的，请稍等20分钟左右"
  },
  "scheduled_at": "2026-06-10T20:00:00+08:00"
}
```

执行结果：

```json
{
  "action_task_id": "AT_001",
  "status": "success",
  "executed_at": "2026-06-10T20:00:03+08:00",
  "result": {
    "message_id": "MSG_001"
  }
}
```

失败结果：

```json
{
  "action_task_id": "AT_002",
  "status": "failed",
  "error_code": "CUSTOMER_DELETED",
  "result": {
    "reason": "客户已删除或无法触达"
  }
}
```

## 7. 最终推荐

建议就按这两个任务同时开工：

```text
任务一：客户状态中台
任务二：动作执行引擎
```

它们之间只通过 `action_task` 拼接。

这样做的好处是：

- 两边可以同时开发。
- 后面任何工具替换都不影响主逻辑。
- RPA、API、视频软件都可以像乐高积木一样替换。
- 客户再多，也只是多一些状态记录和队列任务，不会卡死。
- 后面扩展红包、付款失败、化解咨询，只是新增 action_type 和状态分支。
